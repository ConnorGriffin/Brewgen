# Reproducible runtime dependency lock for the shipped Brewgen API (issue #34).
#
# This is the full resolved runtime closure — direct dependencies plus their
# transitive dependencies — pinned to exact versions so the deployed image is
# measurable against #28's security baseline. Regenerate after a dependency
# change with a clean runtime-only install:
#
#     python3 -m venv .runtimevenv
#     .runtimevenv/bin/pip install .
#     .runtimevenv/bin/pip freeze   # drop the local `brewgen @ file://` line
#
# Python: >=3.11 (see pyproject.toml).
blinker==1.9.0
click==8.4.2
Flask==3.1.3
flask-cors==6.0.5
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
PuLP==3.3.2
python-slugify==8.0.4
text-unidecode==1.3
Werkzeug==3.1.8
